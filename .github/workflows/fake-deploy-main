name: Fake deploy main

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  Fake_deploy_main:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print out "Now deploying to production"
        run: echo "Now deploying to production"
